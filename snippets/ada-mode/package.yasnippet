# -*- mode: snippet; require-final-newline: nil -*-
# name: package
# key: pa
# --
package `(if (string-match "\.adb$"
                           (buffer-name))
           "body " "")`${1:`(capitalize (file-name-sans-extension (buffer-name)))`} is
  $0
end $1;